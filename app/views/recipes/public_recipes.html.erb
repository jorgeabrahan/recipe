<section class="container m-block-2">
  <h1 class="title">Public Recipes</h1>
  <% if @recipes.length < 1 %>
    <p>No public recipes so far!</p>
  <% else %>
    <div class="public-recipes"><%= render partial: 'public_recipe', collection: @recipes, as: :recipe %></div>
  <% end %>
</section>
